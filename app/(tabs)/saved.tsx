import React from 'react';
import { Text, View } from 'react-native';

const Saved = () => {
  return (
    <View className="flex-1 justify-center items-center border-2 border-blue-500" >
      <Text>Saved</Text>
    </View>
  );
};

export default Saved;